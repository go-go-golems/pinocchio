import { ProviderMultiDemoPage } from '../webchat/ProviderMultiDemoPage';

/** Temporary route kept only until CHATOVERLAY-009 Phase 5 deletes or moves provider isolation demo scaffolding. */
export function ProviderMultiDemoRoot() {
  return <ProviderMultiDemoPage />;
}
