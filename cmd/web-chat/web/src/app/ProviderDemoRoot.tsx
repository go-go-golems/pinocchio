import { ProviderDemoPage } from '../webchat/ProviderDemoPage';

/** Temporary route kept only until CHATOVERLAY-009 Phase 5 deletes provider demo scaffolding. */
export function ProviderDemoRoot() {
  return <ProviderDemoPage />;
}
